<%= form_for(@ipabuild,:url => project_ipaapp_ipabuilds_path) do |f| %>

  <div class="inputs">
  	<%= f.file_field :package %>
  </div>

  <div class="actions">
    <%= f.button :submit,:class => 'btn btn-success' %>
  </div>
<% end %>
