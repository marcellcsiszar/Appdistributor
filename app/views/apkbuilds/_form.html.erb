<%= form_for(@apkbuild,:url => project_apkapp_apkbuilds_path) do |f| %>

  <div class="inputs">
  	<%= f.file_field :package %>
  </div>

  <div class="actions">
    <%= f.button :submit,:class => 'btn btn-success' %>
  </div>
<% end %>
